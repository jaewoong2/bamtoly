//revalidateTag.ts
'use server';

import { revalidateTag } from 'next/cache';

//revalidateTag.ts

//revalidateTag.ts

//revalidateTag.ts

export const customRevalidateTag = async (tag: string) => {
  revalidateTag(tag);
};
